apiVersion: v1
kind: Service
metadata:
  name: spgwc-metrics
  namespace: omec
  labels:
    app.kubernetes.io/name: spgwc-metrics
spec:
  clusterIP: None
  ports:
    - name: metrics9089
      port: 9089
      targetPort: 9089
      protocol: TCP
  selector:
    app: spgwc
  type: ClusterIP
